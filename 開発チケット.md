# LiveStack 機能チケットメモ

更新日: 2026-05-19

## チケット1: 位置合わせROIの固定サイズ化

### 目的
- 位置合わせ計算を軽量化し、誤一致を減らす。

### 背景
- 現在は中央80%をテンプレートにしており、探索コストが高い。
- 画面全体に近い領域だと飛行物体の影響を受けやすい。

### 要件
- ROIサイズを固定(例: 384x384 または 512x512)で選択可能にする。
- 画面中央基準でROIを配置する。
- 小解像度時は画像サイズを超えないよう自動縮小する。

### 受け入れ条件
- 従来よりフレーム処理時間が短縮する。
- 固定ROIで通常の星像シーンで位置合わせが継続する。

## チケット2: 飛行物体混入時の誤一致耐性

### 目的
- 飛行機や衛星が写り込んだ際に位置合わせが破綻しないようにする。

### 背景
- ROIに移動物体が入ると、相関のピークが物体に引っ張られる可能性がある。

### 要件
- 複数ROI(3-5点)でそれぞれオフセット候補を推定する。
- 最終オフセットは中央値または多数決で決定する。
- 候補同士のばらつきが大きい場合はそのフレームを不採用にする。

### 受け入れ条件
- 飛行物体が一部ROIに入っても、スタック像が急激に崩れない。
- 不採用フレーム時にスタック数やログが一貫して扱われる。

## チケット3: 外れ値オフセットの除外

### 目的
- 誤マッチによる大ジャンプを抑制する。

### 背景
- 相関値が高くても誤った位置に一致するケースがある。

### 要件
- 直近Nフレームのオフセット履歴を保持する。
- 新オフセットが履歴統計(中央値/MADなど)から逸脱した場合は不採用にする。
- 不採用時はスタック加算しない。

### 受け入れ条件
- 単発の誤一致でスタック像全体が流れない。
- 不採用判定が連続した場合でも処理が停止しない。

## チケット4: 位置合わせデバッグ可視化

### 目的
- 調整しやすくし、誤一致の原因追跡を可能にする。

### 要件
- ROI位置、推定オフセット、相関値、採否理由を表示またはログ出力する。
- デバッグ表示はON/OFF可能にする。

### 受け入れ条件
- 誤一致時に、どのROIが原因か判別できる。
- 通常運用時は表示を無効化して負荷を抑えられる。

## メモ
- C実装の方針がある場合は優先して合わせる。
- 先にC版の ROIサイズ、探索範囲、閾値、失敗時処理 を確認する。

## チケット5: 機能比較表ベースの共通化リファクタリング

### 目的
- READMEの機能比較表を設計図にして、4アプリの共通機能を common 配下に部品化する。
- 部品を差し替えるだけで、比較表の × を段階的に 〇 にできる構造にする。

### 背景
- live_view.py / live_stack.py / raw_live_view.py / raw_live_stack.py で同種の処理が重複している。
- 機能追加のたびに4ファイルへ個別修正が必要になり、差分管理と品質維持のコストが高い。

### 要件
- 機能を以下の単位で common に分離する。
- 入力ソース層: source adapter (picamera2 / cv2.VideoCapture / rpicam-raw TCP)
- 画像処理層: WB、ガンマ、自動ストレッチ、RAWデコード
- スタック層: 加算、位置合わせ、ダーク減算、フレーム採否
- 保存層: PNG/JPEG/FITS/NPY/SER の writer
- UI層: キーマップ、オーバーレイ、設定メニュー、ヒストグラム描画
- 各アプリは「機能フラグ + 部品組み合わせ」で構成する。
- 既存の操作キー互換を維持する。

### 受け入れ条件
- 4アプリ間の共通処理重複行数が目視で明確に減る。
- 新機能を1か所追加した際、最低2アプリ以上で再利用できる。
- 既存の主要操作(q/s/t/h など)が回帰しない。

## チケット6: 比較表からの段階的 ×→〇 展開計画

### 目的
- 一気に全置換せず、低リスクで比較表の × を埋める手順を定義する。

### 要件
- Phase 1: 保存系共通化
- 共通 writer を導入し、PNG/JPEG/FITS/NPY/SER のI/Fを統一する。
- Phase 2: 表示系共通化
- 情報オーバーレイ、ヒストグラム+CCDF、共通キー処理を統一する。
- Phase 3: 処理系共通化
- WB/ガンマ/ストレッチを pipeline 化し、適用順とON/OFFを共通化する。
- Phase 4: スタック系共通化
- 位置合わせ、加算、ダーク処理、外れ値除外を stack engine として共通化する。

### 受け入れ条件
- 各Phase完了ごとに、README機能比較表の説明と実装が矛盾しない。
- Phaseごとのコミットで動作確認範囲が明確になっている。

## チケット7: 共通化前提の機能マニフェスト導入

### 目的
- 各スクリプトの「持っている機能」を宣言し、部品組み合わせを見える化する。

### 要件
- 各アプリで feature manifest を定義する。
- 例: raw=True, stack=True, dark=True, wb=True, gamma=True, ser=True など
- 起動時に manifest を読み、必要な common 部品だけを初期化する。
- README機能比較表の自動生成は将来拡張として検討する。

### 受け入れ条件
- 新しいアプリ派生を作る際、manifest の設定変更だけで基本機能構成を作れる。
- 既存4アプリの機能差分をコード上で即座に把握できる。

## チケット8: SSH連携によるrpicam-raw自動起動とEXIF記録

### 目的
- 受信側（PC）からSSH経由でRaspberry Pi上のrpicam-rawを起動し、シャッター・ゲイン等の設定値をEXIFに記録する。

### 背景
- 現在のTCPストリームは生ピクセルのみで、カメラ設定値（シャッタースピード・ゲイン）がストリームに含まれない。
- `--shutter` / `--gain` を受信側引数として手動入力すれば「指定値」としてEXIF記録は可能だが、SSH連携があれば起動と記録を一元化できる。

### 要件
- `paramiko` でSSH接続し、rpicam-rawコマンドをリモート実行する。
- SSH接続情報として `--ssh-host`, `--ssh-user`, `--ssh-key` を追加引数で受け取る。
- `--shutter`, `--gain` 等のカメラ側パラメータも引数化し、rpicam-rawコマンドに渡す。
- SSH未指定時は従来通りの手動起動モードで動作すること（後方互換必須）。
- 認証はSSH鍵方式のみ対応（パスワード認証は実装しない）。
- アプリ終了時にSSH経由でrpicam-rawプロセスを停止する。

### 受け入れ条件
- SSH指定時に受信側のみのコマンドで送受信が同時に起動できる。
- JPEG/PNG保存時のEXIFにシャッター・ゲインが記録されている。
- SSH未指定時の既存動作が回帰しない。
