# Camera Live Applications 仕様

**生成者:** GitHub Copilot (Agent, Claude Sonnet 4.6)  
**作成日:** 2025年6月15日  
**最終更新:** 2026年5月18日 - RAW専用アプリ分離

## 動作環境
- **ハードウェア**:
  - Raspberry Pi 5 RAM16GB
  - Raspberry Pi Camera imx585（複数カメラ対応、他センサーも自動対応）
- **ソフトウェア**:
  - Raspberry Pi OS Bookworm
  - Python 3.11
- **ストレージ**:
  - Lexar Professional Gold MicroSDXC 128GB

## 開発環境
- **ソフトウェア**:
  - VSCode 1.101.0

## 必要なライブラリ
- `astropy`: FITSファイル保存用。
- `Pillow`: JPEG/PNG保存用。
- `piexif`: EXIFデータ操作用。
- `cv2`: OpenCVライブラリ。

## 処理の概要

### モードについて

#### Live View Mode
- リアルタイムでカメラ映像を表示。
- 加算処理やダークフレーム減算は行わず、シンプルなプレビューを提供。
- 以下の2モードを提供。
  - **ローカルカメラモード**: `--source` 未指定。`picamera2` 経由、カメラ0/1切り替え対応。
  - **URLストリームモード**: `--source 0` または `--source tcp://...` 指定。`cv2.VideoCapture` 経由。
- 共通表示:
  - `fit_to_display()` により最大1920×1080に自動リサイズ（アスペクト比維持）。
  - `draw_info_lines()` で統一フォーマットのオーバーレイ表示（ソース / 解像度 / フレーム数 / min / max / mean）。
  - `[h]` キーでヒストグラム+CCDFオーバーレイのON/OFF。
- **対応メソッド:** `run_stream_mode()`, `main()`

#### RAW Live View Mode (`raw_live_view.py`)
- `rpicam-raw` の生TCPストリーム専用受信・表示モード。
- MIPI CSI-2 パック形式（8/10/12/16bit）を手動アンパック。
- 表示補正:
  - パーセンタイルストレッチ＋ガンマ2.2補正による線形RAWの視認性改善。
  - ホワイトバランス（WB）をデベイヤ後画像に適用（RAWデータ自体は不変）。
  - 白点クリックWB（`[w]`）とB/G/Rスライダーによる手動WBを併用可能。
- 操作:
  - `[b]` キーでBayerパターンをリアルタイム切り替え（最終フレームを即再デコード）。
  - `[a]` キーで自動ストレッチON/OFF。
  - `[r]` キーで16bit RAWデータをNPY形式保存。
  - `[n]` / `[+]` / `[-]` でフォーマット候補切替・skipバイト調整。
  - `[h]` キーでヒストグラム+CCDFオーバーレイのON/OFF。
  - `[w]` キーで白点クリックWBのON/OFF、`[W]` キーでWBリセット。
  - `[s]` 保存はWB適用後画像を保存（ストレッチON時は表示トーン相当）。
- **対応メソッド:** `run_raw_live_view()`, `main()` in `raw_live_view.py`

#### Live Stack Mode
- フレームを加算してノイズを軽減し、画質を向上。
- 最新フレームを基準に過去フレームを位置合わせして加算。
- ダークフレーム減算を実施。
- カメラ切り替えによる複数カメラ対応。
- `--source` 指定時は `run_stream_mode()` で `cv2.VideoCapture` 経路を使用。
- **対応メソッド:** `LiveStack.process_stack()`

### スタックを実現するための処理方法

#### リングバッファ
- **撮影用リングバッファ**: 最大100フレームを保持し、最新フレームを基準に過去フレームを位置合わせ。
  - **対応メソッド:** `LiveStack.add_to_buffer()`
- **ダークフレーム用リングバッファ**: ダークフレームを加算平均して作成。
  - **対応メソッド:** `LiveStack.set_dark_frame()`

#### ダークフレーム作成までの流れ
1. ダークフレーム取得モードで複数フレームを撮影。
2. 撮影したフレームをリングバッファに追加。
3. バッファ内のフレームを加算平均してダークフレームを作成。
- **対応メソッド:** `LiveStack.set_dark_frame()`

#### 撮影画像からダークフレームを引いてスタックする流れ
1. 最新フレームを基準に設定。
2. ダークフレームを減算し、負の値をクリッピング。
3. 過去フレームを位置合わせして加算。
4. スタック数を更新。
- **対応メソッド:** `LiveStack.process_stack()`

#### 位置合わせについて
- 最新フレームを基準にテンプレートマッチングを実施。
- 相関値が閾値を超えた場合、位置合わせを成功と判断。
- アフィン変換を用いて過去フレームを基準フレームに合わせる。
- **対応メソッド:** `LiveStack.add_frame()`

#### スタック数の決定方法
- 有効なフレーム数をカウントしてスタック数を更新。
- 停止条件は `Stop Threshold` と `Stop Ratio(%)` で可変。
- 「次の1フレームを加算した結果」を事前評価し、閾値超過する場合はそのフレームを加算せず停止。
- **対応メソッド:** `LiveStack.process_stack()`

## クラスとメソッド

### `LiveStack` クラス
リアルタイムのフレームスタッキングを管理するクラス。

#### メソッド一覧
- **`__init__(max_frames=100, verbose=False)`**:
  - リングバッファを初期化し、スタック処理を準備。
  - 引数:
    - `max_frames`: リングバッファの最大フレーム数。
    - `verbose`: デバッグ出力の有効化。
- **`reset()`**:
  - スタック状態をリセット。
- **`add_frame(frame)`**:
  - フレームをスタックに追加。
  - 引数:
    - `frame`: 加算するフレーム。
- **`add_to_buffer(frame)`**:
  - フレームをリングバッファに追加。
  - 引数:
    - `frame`: バッファに追加するフレーム。
- **`process_stack()`**:
  - スタック処理を実行。
  - 戻り値:
    - 加算後のフレーム。
- **`set_dark_frame(frame)`**:
  - ダークフレームを加算平均して設定。
  - 引数:
    - `frame`: ダークフレームとして使用するフレーム。

### `SettingsMenu` クラス
設定メニューのUI管理とキー入力処理を行うクラス。

#### メソッド一覧
- **`__init__()`**:
  - 設定項目とデフォルト値を初期化。
- **`handle_key(key_raw)`**:
  - カーソルキーとEnter/ESCキーの入力処理。
  - 引数:
    - `key_raw`: OpenCVの `waitKeyEx()` で取得したキー値。
  - 戻り値:
    - `bool`: メニューがキーを処理した場合True。
- **`change_value(direction)`**:
  - 選択中の設定項目の値を変更。
  - 引数:
    - `direction`: 変更方向（1: 増加, -1: 減少）。
- **`get_exposure_text(exposure_us)`**:
  - 露出時間をわかりやすい文字列に変換。
  - 引数:
    - `exposure_us`: マイクロ秒単位の露出時間。
  - 戻り値:
    - `str`: "1/60"や"2s"形式の文字列。
- **`draw_menu(frame)`**:
  - 設定メニューをフレームに描画。
  - 引数:
    - `frame`: 描画対象のフレーム。
  - 戻り値:
    - `frame`: メニューが描画されたフレーム。
- **`get_current_values()`**:
  - 現在の設定値を辞書で取得。
  - 戻り値:
    - `dict`: 設定値の辞書。
- **`set_current_values(camera, gain, exposure, max_frames, stack_mode, info_display=True)`**:
  - 設定値を更新。
  - 引数:
    - `camera`: カメラ番号。
    - `gain`: ゲイン値。
    - `exposure`: 露出時間（マイクロ秒）。
    - `max_frames`: 最大フレーム数。
    - `stack_mode`: スタックモードのON/OFF。
    - `info_display`: 情報表示のON/OFF。
- **`set_size_choices(size_labels, current_label=None)`**:
  - Size項目の選択肢を更新。
  - 引数:
    - `size_labels`: 利用可能解像度ラベル配列（例: `640x480`）。
    - `current_label`: 現在選択ラベル。

### `CameraConfig` クラス
カメラ設定と初期化を管理するクラス。センサーモードを動的に取得するため、センサー種別ごとのテーブル管理が不要。

#### メソッド一覧
- **`_pick_best_mode(picam2, prefer_small=True)`**:
  - `sensor_modes` から最適な RAW モードを選択して返す。
  - 16bit モード（ISP統計が壊れる）を自動除外。
  - `prefer_small=True` で最小解像度（高fps）、`False` で最大解像度を返す。
  - 戻り値: `{"format": str, "size": (w, h)}` または `None`。
- **`create_camera(camera_num=0, prefer_small=True, buffer_count=1)`**:
  - センサーのネイティブサイズを出力解像度に使用してカメラを作成。クロップなし。
  - 引数:
    - `camera_num`: カメラ番号（0または1）。
    - `prefer_small`: `True` で最小モード（高fps）、`False` で最大モード。
    - `buffer_count`: バッファ数。
  - 戻り値: `Picamera2` 初期化済みカメラオブジェクト。
- **`create_fast_camera(camera_num=0)`**:
  - 高速プレビュー用カメラを作成（`prefer_small=True`）。
  - 引数:
    - `camera_num`: カメラ番号（0または1）。
  - 戻り値: `Picamera2` 初期化済みカメラオブジェクト。
- **`create_high_res_camera(camera_num=0)`**:
  - 最大解像度カメラを作成（`prefer_small=False`）。センサー全面・クロップなし。
  - 引数:
    - `camera_num`: カメラ番号（0または1）。
  - 戻り値: `Picamera2` 初期化済みカメラオブジェクト。
- **`create_low_light_camera(camera_num=0)`**:
  - 低照度撮影用カメラを作成（最小モード・長時間露出設定）。
  - 引数:
    - `camera_num`: カメラ番号（0または1）。
  - 戻り値: `Picamera2` 初期化済みカメラオブジェクト。
- **`get_available_sizes(camera_num=0)`**:
  - カメラごとの利用可能解像度一覧を返す（16bitモード除外、重複なし）。
  - 戻り値: `(w, h)` タプルの配列。
- **`create_camera_with_size(camera_num=0, size=None, buffer_count=1)`**:
  - 指定解像度でカメラを作成（指定不一致時は最小サイズにフォールバック）。
  - 引数:
    - `camera_num`: カメラ番号。
    - `size`: 希望解像度 `(w, h)`。
    - `buffer_count`: バッファ数。
- **`apply_camera_settings(picam2, exposure_time=16667, gain=2.0)`**:
  - カメラの露出とゲインを動的に調整。
  - 引数:
    - `picam2`: `Picamera2` インスタンス。
    - `exposure_time`: 露出時間（マイクロ秒）。
    - `gain`: アナログゲイン値。

### `save_fits(image, filename, metadata)` 関数
FITS形式で画像を保存し、メタデータを付与する関数。
- 引数:
  - `image`: 保存する画像。
  - `filename`: 保存先のファイル名。
  - `metadata`: FITSヘッダーに追加するメタデータ。

### `create_camera_safely(camera_num, gain, exposure, low_light_mode, selected_size)` 関数
エラーハンドリング付きでカメラを安全に作成する関数。
- 引数:
  - `camera_num`: カメラ番号（0または1）。
  - `gain`: ゲイン値。
  - `exposure`: 露出時間（マイクロ秒）。
  - `low_light_mode`: 低照度モードのON/OFF。
  - `selected_size`: 選択解像度 `(w, h)` または `None`。
- 戻り値:
  - `Picamera2`: 初期化済みカメラオブジェクト（失敗時はNone）。

### `main()` 関数
アプリケーションのエントリーポイント。カメラの初期化、フレーム取得、キー入力処理を実行。

#### コマンドライン引数
- `-n` / `--max-frames`: 起動時の最大スタックフレーム数を指定（デフォルト: 100）。設定メニューの Max Frames 初期値にも反映される。
- `--source`: 映像ソースを指定（カメラ番号または `tcp://...` URL）。
- `--flip-h`: 起動時に左右反転を有効化。
- `--flip-v`: 起動時に上下反転を有効化。
  ```bash
  python3 live_stack.py --max-frames 50
  python3 live_stack.py --source 0
  python3 live_stack.py --source tcp://192.168.1.17:8888
  python3 live_stack.py --flip-h --flip-v
  ```

補足:
- `--source` 指定時は `cv2.VideoCapture` 経路を使用。
- `--source` 未指定時は従来の picamera2 経路を優先し、利用不可環境では source=0 へフォールバック。

## キー操作

### 共通操作
- `q`: アプリケーション終了。
- `m`: 設定メニューの開閉。
- `i`: 情報表示のON/OFF切り替え。
- `c`: カメラ切り替え（0と1の間でトグル）。
- `0`/`1`: カメラ番号直接指定。

### Live View Mode
- `s`: 画像保存。
- `d`: ダークフレーム取得。

### Live Stack Mode
- `t`: Live StackモードのON/OFF切り替え。
- `r`: スタックリセット。
- `h`: 左右反転トグル（出力側で即時反映）。
- `v`: 上下反転トグル（出力側で即時反映）。
- `f`: FITS形式で画像保存。
- `j`: JPEG形式で画像保存。
- `p`: PNG形式で画像保存。

### 従来のキー操作（後方互換性）
- `+`/`-`: ゲイン調整。
- `2`〜`9`: シャッター速度変更（`0`/`1`はカメラ番号指定で使用）。

### 設定メニュー操作
設定メニューが開いている時の専用操作：
- **上下矢印**: 設定項目選択（Camera → Size → Gain → Exposure → Max Frames → Stack Mode → Info Display → Stop Threshold → Stop Ratio(%)）。
- **左右矢印**: 選択項目の値変更。
- **Enter**: 変更を確定適用（メニューは開いたまま）。
- **ESC**: 変更を破棄してメニューを閉じる。
- **m**: メニューを閉じる（未確定変更は破棄）。

#### 設定可能項目
1. **Camera**: 0/1 - カメラ番号の切り替え
2. **Size**: 利用可能解像度一覧（カメラごとに異なる）
3. **Gain**: 1.0～8.0（0.5刻み） - カメラ感度
4. **Exposure**: 10秒～1/2000秒（15段階プリセット） - 露出時間
5. **Max Frames**: 1～100（1刻み） - スタッキング最大フレーム数
6. **Stack Mode**: ON/OFF - LiveStackモードの切り替え
7. **Info Display**: ON/OFF - 画面上の情報表示切り替え
8. **Stop Threshold**: 5～255（5刻み） - 閾値超え判定の輝度しきい値
9. **Stop Ratio(%)**: 1～50（1刻み） - 閾値超え割合の停止条件

## 統計オーバーレイ（右上）
- 表示内容: ヒストグラム（緑）+ CCDF（赤）を同時表示。
- ガイド線: 閾値（垂直）と停止比率（水平）を表示。
- 判定値表示: `T`（閾値）, `R`（比率）, `P(X>=T)`（実測比率）をグラフ下段に表示。
- 判定指標: `max(B,G,R)` を使用（表示と停止判定を統一）。
- ヒストグラムは対数正規化（`log1p`）で可視性を改善。

## 表示フレーム制御
- 内部処理・保存用フレームは元解像度を維持。
- プレビュー表示のみ縮小。
  - 画面サイズ取得成功時: 画面の85%以内。
  - 取得失敗時: 高さ600px基準。

## 初期化処理

#### シャッター速度変更時
- ダークフレームとリングバッファを初期化。
  - **対応メソッド:** `main()`

#### ゲイン変更時
- ダークフレームとリングバッファを初期化。
  - **対応メソッド:** `main()`

#### カメラ切り替え時
- 現在のカメラを停止・解放。
- 新しいカメラを初期化。
- ダークフレームとリングバッファを初期化。
- スタック状態をリセット。
- 切り替え失敗時は元のカメラに自動復帰。
  - **対応メソッド:** `main()`

## 注意点
- FITS保存時のメタデータはFITS標準に準拠。
- FITS保存では画面表示のオーバーレイテキストが除外され、元のフレームデータのみが保存される。
- JPEG保存時にはEXIFデータが付与され、PNG保存時には文字が含まれないフレームを保存。
- ダークフレーム取得はLive Viewモードでのみ可能。
- カメラ切り替え時は全てのバッファとスタック状態がリセットされる。
- カメラ切り替え機能は現在0と1の2つのカメラに対応。

## 変更履歴

#### 2026/05/18 RAW Live ViewにWB追加
- **WB機能追加**: `raw_live_view.py` に白点クリックWB（`[w]`）とB/G/Rスライダーによる手動WBを追加。
- **RAW不変の方針**: WBはデベイヤ後画像にのみ適用し、RAWデータそのものは変更しない。
- **保存反映**: `[s]` 保存時はWB適用後画像を保存するよう変更。

#### 2026/05/18 RAW専用アプリ分離
- **RAW機能を分離**: `live_view.py` に統合していたRAW Bayer受信機能を `raw_live_view.py` に分離。
- **責務を明確化**: `live_view.py` はローカルカメラ/URLストリームの軽量プレビュー専用に整理。
- **RAW運用を独立**: `rpicam-raw` 受信、MIPI CSI-2アンパック、Bayer切替、ストレッチ、NPY保存は `raw_live_view.py` で継続。

#### 2026/05/17 live_view.py RAW Bayer統合
- **RAW Bayerモード追加**: `rpicam-raw` の生TCPストリームを受信・デコードして表示する機能を `live_view.py` に統合。
- **3モード統一表示**: ローカルカメラ / URLストリーム / RAW Bayerの全モードで `draw_info_lines()` による共通フォーマット表示（ソース・解像度・フレーム数・min/max/mean/チャンネル別平均）。
- **表示サイズ統一**: `fit_to_display()` を全モードに適用し、常に最大1920×1080以内に自動リサイズ。
- **ヒストグラムON/OFF**: `[h]` キーで全モード共通のヒストグラム+CCDFオーバーレイを切り替え。
- **自動RAWモード検出**: `--stride` または `--bits` 指定時は `--raw` フラグなしでもRAWモードに自動切替。
- **RAW Bayerモード実装**: MIPI CSI-2 10/12bit CSI2Pアンパック、パーセンタイルストレッチ＋ガンマ2.2補正、Bayerパターンリアルタイム切替（`[b]`）、NPY保存（`[r]`）。
- **IMX678対応確認**: Bayer=BGGR、stride=5792、センサー解像度3856×2180を確認済み。

#### 2026/05/10 source対応・停止判定整合
- **source対応**: `--source` を追加し、USBカメラ番号と `tcp://...` URL の両方を受信可能に変更。
- **Windows起動改善**: `picamera2` など依存の遅延import化により、`--source` 利用時の非Raspberry Pi環境起動を改善。
- **メニューキー修正**: `waitKeyEx()` ベースへ変更し、Windowsで矢印キーが正しく機能するよう修正。
- **停止判定の前倒し**: 閾値超過フレームを加算前に判定し、超過するフレームは加算しない方式へ修正。
- **判定指標の統一**: CCDF表示と停止判定を `max(B,G,R)` に統一。
- **閾値範囲更新**: `Stop Threshold` の下限を 127 から 5 に変更（5刻み）。
- **入力ビット幅表示**: 入力フレームの推定ビット幅をオーバーレイ表示。

#### 2026/03/14 機能追加
- **コマンドライン引数対応**: `-n` / `--max-frames` オプションで起動時に最大スタックフレーム数を指定可能（デフォルト: 100）。
- **フリップ機能追加**: `--flip-h`（左右反転）・`--flip-v`（上下反転）で起動時の反転を指定。`h`/`v`キーでトグル切り替えも可能。反転は出力側（`save_frame`/`display_frame`）で適用するため即時反映。指定値は設定メニューの Max Frames 初期値にも反映。

#### 2026/02/28 統計表示・停止条件調整
- **ヒストグラム追加**: 統計グラフ（ヒストグラム + CCDF）を追加し、閾値/比率判定を可視化。
- **停止条件の調整機能**: `Stop Threshold` と `Stop Ratio(%)` を追加。
- **表示/操作の改善**: グラフ可読性を改善し、`Enter` は適用のみ（閉じない）に変更。
- **共通部品化**: 統計オーバーレイ描画を `common/hist_overlay.py` に集約し、`live_view.py` と `live_stack.py` で共有。

#### 2026/02/28 Size選択・表示分離
- **Size選択方式**: `FAST/HIGH` 2択を廃止し、カメラごとの利用可能解像度一覧から選択可能に変更。
- **適用操作の統一**: 設定は `Enter` で確定適用、`ESC`/`m` で未確定変更を破棄。
- **表示画像の分離**: 内部処理画像と表示画像を分離し、表示のみ自動縮小。

#### 2026/02/21 センサー自動対応
- **センサーモード自動検出**: `picamera2.sensor_modes` を使用し、センサーごとの RAW フォーマット/サイズをコード変更なしで自動取得。
- **16bitモード自動除外**: ISP統計が壊れる `SRGGB16` 等を自動的に除外し、映像が真っ黒になる問題を恒久的に解決。
- **クロップなし出力**: 出力解像度をセンサーのネイティブサイズに合わせることで、libcamera による自動クロップ（上が切れる問題）を解消。
- **ダークフレームのサイズ管理改善**: 初期値を `None` にして実フレームのサイズで動的に確保。サイズ不一致によるクラッシュを防止。
- **新センサー追加時のコード変更不要**: テーブルへの手動追記が不要になり、接続するだけで自動対応。

#### 2025/12/02 バグ修正
- **保存フレームの分離**: `save_frame` と `display_frame` を完全に分離し、保存される画像にテキスト情報が重ねられないように修正。
- **Stackモード保存の修正**: `process_stack()` で処理されたスタック済みフレームが正しく保存されるように修正。
- **全保存形式の統一**: JPEG、PNG、FITS保存時に全て `save_frame` を使用し、テキストオーバーレイのないクリーンな画像データを保存。

#### 2025/07/01 カメラ切り替え・設定メニュー追加
- **カメラ切り替え機能**: `c` キーでカメラ0と1の間でトグル切り替え、`0`/`1` キーで直接カメラ番号指定が可能。
- **設定メニューにカメラ項目追加**: 設定メニューからカメラ番号の切り替えが可能。
- **カメラ切り替え時の安全性向上**: 切り替え失敗時は元のカメラに自動復帰、バッファとスタック状態を自動リセット。
- **情報表示切り替え機能**: `i` キーで画面上のテキスト表示をリアルタイムでON/OFF可能。
- **FITS保存の改善**: LiveStackモードではスタック済み画像データ、LiveViewモードでは元フレームデータを保存し、いずれもオーバーレイテキストは除外。
- **双方向同期**: キー操作と設定メニューの全設定項目が相互に同期。
